# ─────────────────── Core Web Framework ───────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# ─────────────────── Database ─────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# ─────────────────── Pydantic / Settings ──────────────────
pydantic==2.11.3
pydantic-settings==2.8.1

# ─────────────────── Pipecat ──────────────────────────────
# sarvam  → sarvamai SDK (STT/TTS/LLM via SarvamLLMService — added in 0.0.108)
# webrtc  → aiortc>=1.14.0 + opencv (SmallWebRTC transport)
# openai  → included in pipecat-ai base deps
# groq    → groq Python SDK (required by pipecat GroqLLMService)
pipecat-ai[sarvam,webrtc,groq]==0.0.108
groq>=0.9.0

# ─────────────────── Voice / Audio ────────────────────────
# edge-tts is used by WhisperEdgePipeline only
edge-tts==6.1.12

# ─────────────────── HTTP Client ──────────────────────────
httpx==0.27.2
aiofiles==24.1.0

# ─────────────────── Data Processing ─────────────────────
pandas==2.2.3
openpyxl==3.1.5
numpy==1.26.4

# ─────────────────── Auth / Security ──────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─────────────────── Utilities ────────────────────────────
python-dateutil==2.9.0
pytz==2024.1
