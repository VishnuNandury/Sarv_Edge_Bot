# For single-service deploy (FastAPI serves the frontend):
# Leave empty — JS will call /api/* on the same host
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=

# For local dev (two separate processes):
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
